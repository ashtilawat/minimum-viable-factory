/**
 * Default export for the @modal parallel route slot.
 *
 * Returning null ensures nothing is rendered in the modal slot
 * when no card detail URL is active.
 */
export default function ModalDefault() {
  return null;
}
